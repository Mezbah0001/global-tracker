# Copy to .env and fill in values

# Server port (default: 3001)
PORT=3001

# Optional: OpenSky credentials for higher rate limits
# Without these, anonymous access is used (400 API credits/day)
# OPENSKY_USER=your_username
# OPENSKY_PASS=your_password
